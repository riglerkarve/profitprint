# dependencies / build output
node_modules/
sites/content-site/dist/
sites/content-site/.astro/
product/pricing-spreadsheet/dist/
_site/

# python
__pycache__/
*.pyc

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# npm lockfile deliberately not committed: CI installs with --legacy-peer-deps and
# the current deploy works; a lockfile with platform-specific optional deps is a
# known source of cross-platform CI breakage. Revisit if builds start drifting.
sites/content-site/package-lock.json
